package lookup;

/**
 * Builds and updates a lexDict based on sample data
 */
public class DictUpdater {
    LexDict lexDict;

    public DictUpdater(LexDict lexDict) {
        this.lexDict = lexDict;
    }

/*To do:
    Given LixDict, and path to sample
    Update LexDict
    */


}
